<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 配置mybatis的环境 -->
    <environments default="mysql">
        <environment id="mysql">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mysql2"/>
                <property name="username" value="root"/>
                <property name="password" value="root"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 配置mysql的环境 -->
    <!-- 配置事务的类型 -->

    <!-- 配置连接数据库的信息：用的是数据源(连接池) -->



    <!-- 告知mybatis映射配置的位置 -->
    <mappers>
        <mapper resource="com/lengkai/dao/IUserDao.xml"></mapper>
        <mapper resource="com/lengkai/dao/AccountDao.xml"></mapper>
        <mapper resource="com/lengkai/dao/IRole.xml"></mapper>
    </mappers>
</configuration>